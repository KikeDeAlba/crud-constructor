from setuptools import setup, find_packages

setup(
    name='crud-constructor',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        'mysqlclientpy'
    ],
    url='https://github.com/KikeDeAlba/crud-constructor'
)